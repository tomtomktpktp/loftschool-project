include ./mixins.pug

doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title Tomiris's portfolio
	body
		.wrapper
			.maincontent
				block content
		include ./templates/entry.pug